# nvim (nixvim)

Standalone nixvim configuration. The entry point is
[`config/default.nix`](./config/default.nix); add or modify nix files in
`./config` and remember to import them from `default.nix`.

## Testing

```
nix run .
```
