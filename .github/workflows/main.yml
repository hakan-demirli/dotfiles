name: "Nix Build and Test"
on:
  push:
    branches: [main]
  workflow_dispatch:
  workflow_call:
permissions:
  contents: read
jobs:
  checks:
    uses: hakan-demirli/infra-lib/.github/workflows/checks.yml@e136b2e53839728642fb09a5729df5da58ebc0dd
  external-builds:
    uses: hakan-demirli/infra-lib/.github/workflows/external-builds.yml@e136b2e53839728642fb09a5729df5da58ebc0dd
  flake-check:
    needs: [checks, external-builds]
    if: always()
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - env:
          RESULTS: ${{ toJSON(needs) }}
        run: jq -e 'length == 2 and all(.[]; .result == "success")' <<< "$RESULTS"
